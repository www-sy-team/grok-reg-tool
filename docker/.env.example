# Web UI 暴露端口
WEB_PORT=6657

# Web 鉴权默认账号密码会打印在 docker logs 中：admin/admin
# 首次登录后 WebUI 会强制修改账号密码

# 注册轮数
RUN_COUNT=10

# 邮件后端（兼容 dreamhunter2333/cloudflare_temp_email）
MAIL_API_BASE=
MAIL_ADMIN_AUTH=
MAIL_DOMAIN=

# 可选代理
HTTP_PROXY=
BROWSER_PROXY=
